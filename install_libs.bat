@echo off
echo Default python usage - environment variables already set- 
echo if setup fails, repair python by reinstalling it, and set environment variable addition to on when doing so
cd C:\Users
python -m pip install panda
pause